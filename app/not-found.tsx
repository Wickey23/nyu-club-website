import Link from "next/link";
import { Footer, Header } from "./components/SiteChrome";
import { getLiveSiteContent } from "./lib/liveSiteContent";

export default async function NotFound(){const site=await getLiveSiteContent();const brand=site.settings.shortName||site.settings.clubName;const config=(site.settings.cmsConfig||{}) as any;const message=config.website?.custom404||"The link may be old, or the page may have moved.";return <main><Header/><section className="page-section"><div className="wrap" style={{minHeight:"58vh",display:"grid",placeItems:"center",textAlign:"center"}}><div><span className="kicker">404</span><h1 style={{fontFamily:"var(--font-display)",fontSize:"clamp(3.4rem,10vw,7rem)",margin:"10px 0"}}>We couldn&apos;t find that page.</h1><p style={{maxWidth:620,margin:"0 auto 24px",lineHeight:1.7}}>{message} Head back to {brand} or check the latest events.</p><div className="actions" style={{justifyContent:"center"}}><Link className="btn red" href="/">Go home</Link><Link className="btn outline" href="/events">View events</Link></div></div></div></section><Footer/></main>}
